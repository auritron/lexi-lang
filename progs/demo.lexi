# compute area of a circle
func area(param float64) -> float64 {
    const var float64 pi = 3.14159
    var float64 result = pi * radius * radius

    if result >= 10 {
        return "Area is ${result$}"
    } else {
        return "Small area: ${result$}"
    }
}
